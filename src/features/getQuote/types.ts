export interface Steps {
  number: string;
  title: string;
  description: string;  
  showArrow: boolean;
}
export interface FormData {
  name: string;
  phoneNumber: string;
  email: string;
  service: string;
}

